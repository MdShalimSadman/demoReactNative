import {Text, Button, View} from 'react-native';

export const Profile = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
        <Text style={{fontSize: 30, color: '#000000'}}>Profile</Text>
        </View>
    );
};
